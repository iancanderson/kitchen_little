- unless user_signed_in?
  = link_to "Sign up now!", new_user_registration_path, :class => "signup_button round"
%h1 Leaderboard
%table#users
  %tr
    %td Username
    %td Kitchen Efficiency Score
  - @users.each do |user|
    = render(user)